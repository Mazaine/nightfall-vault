global:
  scrape_interval: 30s
  evaluation_interval: 30s
rule_files: [/etc/prometheus/nightfall-alerts.yml]
scrape_configs:
  - job_name: nightfall-api
    metrics_path: /health/metrics
    static_configs:
      - targets: [backend:8000]
